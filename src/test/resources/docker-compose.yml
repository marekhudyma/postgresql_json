version: '3.5'
services:
  postgres:
    image: 'postgres:11.6'
    environment:
      POSTGRES_DB: 'postgresql-json'
      POSTGRES_USER: 'postgres'
      POSTGRES_PASSWORD: 'postgres'
    ports:
      - '5432'